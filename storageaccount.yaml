apiVersion: storage.azure.upbound.io/v1beta2
kind: Account
metadata:
  name: stcrossplanelab01ab
spec:
  forProvider:
    resourceGroupName: rg-crossplane-lab01
    location: eastus
    accountTier: Standard
    accountReplicationType: LRS
    tags:
      environment: lab
      managed-by: crossplane
  providerConfigRef:
    name: default
